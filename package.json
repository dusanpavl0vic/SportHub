{
    "name": "sporthub",
    "version": "1.0.0",
    "scripts": {
        "db": "docker-compose up -d",
        "db-stop": "docker-compose down",
        "app": "pnpm --prefix app run start:dev",
        "app-stop": "pnpm --prefix app run stop"
    },
    "devDependencies": {
        "docker-compose": "latest",
        "nestjs": "latest"
    },
    "dependencies": {
        "@nestjs/common": "^11.0.10"
    }
}
